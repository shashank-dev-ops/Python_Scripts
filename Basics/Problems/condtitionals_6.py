order_size = "Medium"
extra_shot = True

if extra_shot:
    coffee = order_size + " coffee with extra shot"
else:
    coffee = order_size + " coffee without extra shot"

print("order is:", coffee)
# The code above is a simple conditional statement that checks if the variable `extra_shot` is True or False.